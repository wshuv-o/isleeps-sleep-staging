# HAG-Net: Hemispheric Asymmetry-Guided Hybrid Network for Interpretable Sleep Staging

Paper `paper/main.pdf`. Model `model/hag_net.py`; training `model/train_hagnet.py`.
Deep/classical/ensemble baselines in `train/`; benchmark + analysis results in `results/`.

## Data (single shared folder — not duplicated)
All data lives in one place at the parent level: **`../data/`**
- `../data/processed7/`  — 7-channel processed staging data
- `../data/Dataset/`     — raw iSLEEPS EDFs
